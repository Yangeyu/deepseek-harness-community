# 项目开发指南

> 本文件位于仓库根，会话工作在本仓库（含子目录）时由 `dsh-agent-instructions` 自动注入——每位进入本仓库的 dscode 会话第一步就会读到，无需任何配置。
> 本文件为**项目开发者**服务，关注架构、设计与开发流程。
> 工具使用/配置指引见 `guides/AGENTS.md`（部署到 `~/.dsh/AGENTS.md` 供终端用户使用）。

## 项目文档目录

| 文档 | 何时读它 |
|---|---|
| `docs/tui-architecture.md` | 架构、所有权边界、设计惯例问题 |
| `docs/tui-product-roadmap.md` | 里程碑与规划现状 |
| `docs/tui-v0.1.x-design.md` | 目标版本的生命周期/协议设计细节 |

细节一律以 `docs/` 下的文档为单一事实源——需要时用 read 工具按需加载，不要凭记忆回答细节问题。

## 身份与构造

- dscode 是 DeepSeek Harness 的社区构建（DeepSeek Harness Community 终端客户端），会话运行在 Harness profile `tui` 上（开发隔离用 `tui-dev`，由 `pnpm dev` 使用）。
- profile 清单：`~/.dsh/profiles/tui/package.json` 的 `dsh.profile.bundles` = `@deepseek-ai/dsh-base` + `@vascent/deepseek-harness-tui`；用户级补丁层是相邻的 `cordis.patch.yml`。
- 源码仓库中社区 bundle 的补丁是 `packages/tui/cordis.patch.yml`（persona、社区 provider 挂载、TUI 端配置都在这里）。

## 开发与测试

- `pnpm install`：安装依赖
- `pnpm test`：运行测试
- `pnpm typecheck`：类型检查
- `pnpm dev`：开发模式（使用 tui-dev profile）
- `pnpm check`：完整检查（runtime:check + build + lint + typecheck + test）

## 工程规范与约束

本仓库对 coding agent 的执行纪律有强约束。下列条目是**必须遵循**的硬规则，不是建议——违反即视为不合格交付。

### 1. 辩证处理用户输入，禁止盲从

- 对用户的补充与指令做独立、客观的分析与质疑，而不是全盘接受。
- 发现指令与目标冲突、事实存疑、需求模糊或存在风险时，**主动抛出来与用户讨论**，给出你的判断与替代方案。
- 禁止"静默消化"：不得为了迎合而默默吞下有问题的前提，也不得在未对齐的情况下擅自改方向。
- 拿不准时提问，不要在沉默中替用户做重大决策。

### 2. 改动前先量化影响面，重构必须做减法

- 任何改动先在脑中/注释里厘清影响面：触及哪些模块、调用链、契约与测试，再动手。
- **重构 = 清脏**，不是挪代码：移除死代码、冗余分支、无人调用的链路、过期/误导注释；不得"改了功能却保留旧逻辑、旧分支、旧 TODO"。
- 禁止制造多条件分支堆叠来兜旧行为；能用统一路径表达的不要新增 if/else 岔路。
- 清理范围与风险**及时同步给用户**，尤其是跨模块或可能改变外部行为的改动。

### 3. 单元测试站在功能语义上，拒绝防御性堆叠

- 测试应覆盖**功能行为**（给定输入得到约定输出/副作用），而非为"代码曾经存在过"记账。
- 禁止流水式补测试：不要因为改了一行就追加一堆边界断言来"证明没坏"。
- 功能被移除时，**同步删除对应的单元测试**；禁止用新断言去"防御性验证一个已被要求移除的逻辑"——那是把死代码写进测试里。
- 一条测试只为一个功能意图存在；重复的、只为凑覆盖率的用例应直接删。

### 4. 默认做减法，不一味追加

- 接到需求先想"能否用现有能力满足 / 能否合并 / 能否删掉反而更清晰"，再想"要不要新增"。
- 优先复用与收敛，避免为了一个边缘情况引入新抽象、新配置、新依赖。
- 当一个文件/模块/配置的增长速度明显快于其价值，应当提出裁剪方案而不是继续堆。

### 5. 完成后主动暴露异味，禁止静默交付

- 交付时除说明"做了什么"，还必须点出：**仍不合理的地方、代码异味、遗留的技术债、我没能彻底清理的脏链路**。
- 不要为了显得干净而隐藏权衡；把"我不确定的点"和"建议后续处理的项"显式列出。
- 用户没有要求静默，所以任何"将就"决定都要摊开，由用户拍板。

> 一句话准则：**先质疑、先减量、先暴露，再动手；宁可多一次对齐，不要多一份静默的将就。**

## 维护

本文件与 `docs/` 文档随版本演进同步更新——文档新增/改名时先改这个目录表，避免入口失效。